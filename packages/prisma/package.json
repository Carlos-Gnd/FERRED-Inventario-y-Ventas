{
  "name": "@ferred/prisma",
  "version": "1.0.0",
  "scripts": {
    "generate": "prisma generate",
    "migrate:local": "prisma migrate dev --schema=./schema.prisma",
    "migrate:cloud": "DATABASE_URL=$SUPABASE_URL prisma migrate deploy",
    "seed": "ts-node seed.ts"
  },
  "devDependencies": {
    "prisma": "^5.0.0"
  },
  "dependencies": {
    "@prisma/client": "^5.0.0"
  }
}
