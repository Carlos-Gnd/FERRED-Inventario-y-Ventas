{
  "name": "ferred-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "pnpm --parallel --filter @ferred/electron --filter ferred-sync --filter @ferred/server dev",
    "build": "pnpm --recursive build",
    "test": "pnpm --recursive test"
  }
}
